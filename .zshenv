#==========================================================================
# 環境変数（非インタラクティブシェルでも読み込まれる）
#==========================================================================

# Homebrew
eval "$(/opt/homebrew/bin/brew shellenv)"

# Cargo (Rust)
[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"
